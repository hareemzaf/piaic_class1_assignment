//print names
const names: string[] = ["unzila","faria","rimsha","saima"];

//each person's name
for (const name of names ){
    console.log(name);
}

//greetings
for(const name of names) {
    console.log(`hello,${name}! Assalam walikum, kia haal hain.`);
}